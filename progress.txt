# Trae Mod - 项目进度

> Trae IDE 定制框架 - 解锁更多可能

## 2026-04-19 下午

### 🎉 重大架构升级: 动态规则系统上线

**问题背景**:
- 原有 AGENTS.md 硬编码所有协作规则（~128行）
- 新会话 AI 可能忽略或不完全遵守规则
- 修改规则成本高、维护困难、不可移植

**解决方案**: 三层架构的动态规则系统

#### 核心组件

1. **rules/ 规则仓库** (新增目录)
   - `rules/core.yaml` — 4 条核心必读规则
   - `rules/workflow.yaml` — 3 条工作流程规则
   - `rules/git.yaml` — 4 条 Git 提交规范
   - `rules/safety.yaml` — 4 条安全规则
   - 共 **15 条结构化规则**，完整迁移自原 AGENTS.md

2. **scripts/rules-engine.ps1** 规则引擎 (新增脚本, ~470行)
   - 纯 PowerShell 实现，无外部依赖
   - 功能：YAML 解析 → 过滤 → 排序 → Markdown 格式化输出
   - 支持 4 种模式：默认输出 / --check / --list / --output
   - 执行时间 < 1 秒（15 条规则规模）

3. **AGENTS.md 重构为路由器**
   - 从 ~128 行缩减至 **43 行**（减少 66%）
   - 移除所有硬编码规则细节
   - 只保留强制性引导指令和规则引擎调用方式
   - 设计哲学："极简入口 + 动态加载"

4. **docs/dynamic-rules-system.md** 使用指南 (新增文档, ~556行)
   - 8 个章节：快速开始 / 架构详解 / Schema 参考 / 操作教程 / 故障排除 / 迁移指南
   - 包含完整的 YAML 字段说明、示例代码、最佳实践
   - 提供"迁移到其他项目"的 step-by-step 教程（预计 30 分钟）

#### 关键成果

| 指标 | 数值 |
|------|------|
| 规则数量 | 15 条（4 个类别） |
| 代码行数 | ~470 行（纯 PowerShell） |
| 文档行数 | ~556 行（使用指南） |
| AGENTS.md 瘦身 | 66%（128→43 行） |
| 可移植性 | ⭐⭐⭐⭐⭐（30 分钟迁移） |

#### 技术亮点

- **零外部依赖**: 规则引擎使用纯 PowerShell 正则解析器处理 YAML
- **三层分离**: 路由器(AGENTS.md) → 引擎(rules-engine.ps1) → 仓库(rules/*.yaml)
- **配置驱动**: 所有规则通过 YAML 定义，支持启用/禁用、优先级排序
- **向后兼容**: 原 AGENTS.md 内容已完整迁移，无信息丢失

#### 下一步计划

- [ ] 在实际多会话场景中测试路由器机制的有效性
- [ ] 收集反馈并优化规则引擎性能
- [ ] 考虑将此系统抽象为独立的可移植工具包
- [ ] 探索规则版本控制和回滚机制

## 2026-04-18

### 项目初始化

**Trae Mod** 正式成立，定位为 Trae IDE 定制框架。

核心目标：通过源码修改解锁 Trae IDE 的更多能力，建立可持续扩展的定制平台。

### 首批成果

#### 成果 1: 命令自动确认
- **功能**: AI 执行危险命令时无需手动确认
- **涉及补丁**: `auto-confirm-commands`, `service-layer-runcommand-confirm`
- **详细文档**: [docs/achievements/auto-command-confirm.md](docs/achievements/auto-command-confirm.md)

#### 成果 2: 突破思考上限
- **功能**: 模型思考超限时自动续接
- **涉及补丁**: `auto-continue-thinking`, `efh-resume-list`
- **详细文档**: [docs/achievements/auto-continue-thinking.md](docs/achievements/auto-continue-thinking.md)
- **⚠️ 状态**: 补丁已应用，**尚未实际测试**，需要触发长任务验证

### 架构关键洞察

- **服务层补丁才有效** — PlanItemStreamParser 是 SSE 流解析器，不依赖 React 渲染
- **React 组件会冻结** — 切换窗口后组件全部暂停，组件内修改无效
- **模块化设计** — 每个功能独立为补丁，可按需启用

### 技术成果

1. **源码定位**: `D:\apps\Trae CN\resources\app\node_modules\@byted-icube\ai-modules-chat\dist\index.js` (~87MB)
2. **补丁系统**: definitions.json + apply/rollback/verify 脚本
3. **备份机制**: 按时间戳自动备份，支持一键回滚

### 探索历程

1. 定位 Trae 安装路径 + 发现解包状态
2. 通过 NLS 翻译文件定位核心模块
3. 分析 PlanItemStreamParser SSE 流解析逻辑
4. 追踪思考次数上限完整链路
5. 构建补丁系统框架

### 待探索/验证

- ~~触发长运行任务观察思考续接效果~~
- 观察是否会形成重试循环
- 自定义主题/光标样式
- 解除其他 UI 限制

## 2026-04-18 下午

### Token 安全配置

- 将 GitHub CLI 的 token 从全权限 OAuth token 切换到限权 Classic PAT
- 新 token 只有 `repo` 权限（删不了仓库）
- 有效期 90 天
- **不再在项目中记录任何密钥信息**

### 文档结构整理

- 统一目录结构：achievements/、architecture/、guides/
- 修复 AGENTS.md 中的文档路径错误
- 新增"新会话开始前必读"提示

## 2026-04-19

### 新发现: sandbox_execute_failure 弹窗根因

**问题**: AI 执行 python 命令时出现 "命令在沙箱中运行不通过，是否仍然要在沙箱外运行？" 弹窗

**根因分析**:
- `getRunCommandCardBranch` 方法中 WHITELIST 模式有独立分支逻辑
- 当 `block_level = "sandbox_execute_failure"` 时触发 `V2_Sandbox_Execute_Failure` 弹窗
- 原有的 `bypass-runcommandcard-redlist` 补丁只处理了 `ALWAYS_RUN` 和 `default` 模式
- WHITELIST 模式完全没被处理！

**补丁修复**:
- 新增 `bypass-whitelist-sandbox-blocks` 补丁
- 位置: ~8069700
- 效果: WHITELIST 模式下所有沙箱 block 分支都直接返回 `P8.Default`（自动执行）
- 修复后: python 等命令在沙箱模式下应该零弹窗自动执行

**指纹检测修复**:
- 原 `check_fingerprint` 使用了原代码中存在的字符串，导致误判为"已应用"
- 已修改为替换后的唯一字符串 `case Cr.AutoRunMode.WHITELIST:return P8.Default;`

### 架构新认知

| 确认模式 | 原行为 | 补丁后 |
|---------|--------|--------|
| WHITELIST + sandbox blocks | 弹窗确认 | 直接执行 |
| ALWAYS_RUN + RedList | 弹窗确认 | 直接执行 |
| default + RedList | 弹窗确认 | 直接执行 |

## 2026-04-19 上午

### 新功能: 绕过循环检测限制

**问题**: 模型长运行时出现 "检测到模型陷入循环，为避免更多消耗已主动中断对话，建议更换描述后重试" 错误并中断对话

**根因分析**:
- 服务端检测到模型重复工具调用或内容循环时返回错误码
- 错误码: `LLM_STOP_DUP_TOOL_CALL (4000009)` - 重复工具调用循环
- 错误码: `LLM_STOP_CONTENT_LOOP (4000012)` - 内容循环
- 这两个错误码不在"可继续"错误列表(J变量)中 → 只显示错误消息，无"继续"按钮
- 对话被强制中断，用户无法续接

**补丁方案**: `bypass-loop-detection`
- 位置: ~8696378 (J变量定义)
- 原始: `J=!![kg.MODEL_OUTPUT_TOO_LONG,kg.TASK_TURN_EXCEEDED_ERROR].includes(_)`
- 替换为: `J=!![... ,kg.LLM_STOP_DUP_TOOL_CALL,kg.LLM_STOP_CONTENT_LOOP].includes(_)`
- 效果: 循环检测错误被识别为"可继续"错误 → 自动触发 auto-continue-thinking 补丁 → 无感发送"继续"消息 → 模型无缝续接

**工作原理**:
1. 服务端返回 4000009/4000012 错误
2. 客户端 J变量检查通过（因为已加入列表）
3. 触发 if(V&&J) 分支（auto-continue-thinking 已修改此分支）
4. setTimeout(50ms) 自动点击"继续"
5. return null 不显示 Alert 弹窗
6. 模型收到"Continue"消息后无缝继续工作

### 新功能: 服务层确认状态同步更新 (修复切窗口沙箱卡住)

**问题**: 沙箱命令可以自动跑到沙箱外了，但切换到其他会话窗口后又被阻塞，需要重新打开窗口才自动执行

**根因分析**:
- `service-layer-runcommand-confirm` 补丁在服务层调用了 `provideUserResponse({decision: "confirm"})`
- 但**没有更新本地 store 中 planItem 的 `confirm_info.confirm_status`**
- React 组件读取 store 中的状态 → 仍然是 `"unconfirmed"` → 继续显示"等待操作"
- 切换窗口后 React 冻结 → 即使服务端已执行命令，UI 仍卡住

**补丁方案**: `service-layer-confirm-status-update`
- 位置: ~7503400 (service-layer-runcommand-confirm 补丁内)
- 在 `provideUserResponse` 调用后追加: `e.confirm_info&&(e.confirm_info.confirm_status="confirmed")`
- 效果: 服务层确认后立即将 planItem 的确认状态改为 "confirmed"
- 后续 SSE 流处理时，planItem 已经是 confirmed 状态 → React 组件不会显示"等待操作"

**关键架构洞察**:
- 之前以为服务层补丁就够了 → 但忽略了**本地状态同步**
- `updateRunCommandToolStatusFromUser` 中 Confirmed 分支会更新本地 planItem 状态
- 我们的服务层补丁只调了 API，没更新本地状态 → 状态不同步
- 解决方案: 在服务层补丁中也更新 planItem 的 confirm_info

### 新功能: 精准化自动确认过滤 (修复误判用户问答)

**问题**: 自动确认太激进 — AI 向用户提问的选项也被自动"确认"了，用户无法手动选择

**根因分析**:
- `auto-confirm-commands` 和 `service-layer-runcommand-confirm` 两个补丁会自动确认**所有** `confirm_status === "unconfirmed"` 的项目
- 包括 AskUserQuestionCard（`toolName = "response_to_user"`）— 用户问答类型
- 导致 AI 列出的选项全部被自动点击"下一步"/"提交"，用户无法参与选择

**技术发现**: ToolCallName 完整枚举 (位置 ~41400)
| 枚举值 | 字符串值 | 类型 |
|--------|---------|------|
| `RunCommand` | `"run_command"` | 命令执行 ✅ 需要自动确认 |
| `ResponseToUser` | `"response_to_user"` | 用户问答 ❌ 不应自动确认 |
| 其他 30+ 种 | ... | 文件操作/搜索/MCP 等 |

**补丁方案**: v3 精准过滤
- **`service-layer-runcommand-confirm` (v3)**: 增加白名单 `(e?.toolName==="run_command")`
  - 只允许 `run_command` 类型自动确认
  - `response_to_user` 被排除 → AskUserQuestion 正常显示 UI
- **`service-layer-confirm-status-update` (v2)**: 同步增加白名单过滤
- **`auto-confirm-commands` (v2)**: 增加黑名单 `(e?.toolName==="response_to_user")`

**效果**:
- ✅ RunCommandCard → 自动确认（白名单通过）
- ✅ AskUserQuestionCard → 显示 UI 让用户选择（被排除）
- ⚠️ 其他工具类型 → 默认不自动确认（保守策略）

### v2 策略调整: 黑名单模式

**用户反馈**: 白名单太保守，其他工具也需要手动确认
**变更**: `e?.toolName==="run_command"` → `e?.toolName!=="response_to_user"`
**效果**: 只排除 response_to_user，其余所有工具类型默认自动确认

### 主动性全面扫描结果 (2026-04-19)

**完整报告**: [scan-report-2026-04-19.md](docs/reports/scan-report-2026-04-19.md)

**摘要**: 扫描发现 30 个 Alert 弹窗点、7 个错误码枚举、5 个 BlockLevel、35+ 个 ToolCallName。
- ✅ BlockLevel: **5/5 已覆盖**
- ✅ 错误码: **3/7 已处理** (4000002/4000009/4000012)
- ❌ 待处理: MODEL_PREMIUM_EXHAUSTED、CLAUDE_MODEL_FORBIDDEN、INVALID_TOOL_CALL 等

## 2026-04-20

### 🔥 紧急修复: 补丁崩溃导致聊天窗口消失

**问题**: 应用 `auto-confirm-commands` v2 + `service-layer-runcommand-confirm` v5 后，整个 AI 聊天窗口消失（React 组件树崩溃）

**根因分析** (3个问题):

1. **`this` 绑定错误** (高概率主因): `service-layer-runcommand-confirm` v5 的 `.catch()` 使用了普通函数 `.catch(function(e){this._logService...})`，严格模式下 `this` 为 `undefined`，Promise reject 时抛出未捕获 TypeError → 组件树崩溃
2. **`return` 改变控制流** (中概率): `auto-confirm-commands` v2 在 `if(!r)` 块中添加了 `return` 语句，提前退出整个方法
3. **双重调用风险** (中概率): 两个补丁可能对同一 toolcall 都调用 `provideUserResponse`

**修复方案**:

- `service-layer-runcommand-confirm` v5→v6:
  - `.catch(function(e){...})` → `.catch(e=>{...})` (箭头函数修复 this 绑定)
  - 新增 `e?.confirm_info?.confirm_status!=="confirmed"` 守卫防双重调用

- `auto-confirm-commands` v2→v3:
  - 移除 `return` 语句，改为 `if(r){provideUserResponse;confirm_status_sync}else{warn}` 结构
  - 新增 `e.confirm_info&&(e.confirm_info.confirm_status="confirmed")` 同步更新

**额外修复**:

- `check_fingerprint` 缺少 `)` 导致指纹检测失败 → 补丁被重复应用
- 修复后 apply-patches.ps1 正确检测已应用补丁并跳过

### rollback.ps1 参数 Bug 修复 + UX 优化

**问题**: 帮助文档写 `--list`/`--date` 但实际参数是 `-List`/`-Date`

**修复**:
- 修正帮助文档
- 新增 `-Latest` 快捷参数（回滚到最新备份）
- 无参数运行时交互式选择 backup
- `-Date` 支持模糊匹配（部分字符串即可）
- 验证逻辑改进（只检查已启用补丁）

### 源码架构深度解读

**动机**: 补丁开发一直"盲人摸象"，需要系统性架构文档

**产出** (4份架构文档):
- `docs/architecture/sse-stream-parser.md` — SSE 流解析系统完整生命周期
- `docs/architecture/command-confirm-system.md` — 命令确认系统双层架构
- `docs/architecture/limitation-map.md` — 限制点地图（17个Alert点、7个错误码、6个BlockLevel）
- `docs/architecture/module-boundaries.md` — 模块边界与依赖关系图

**关键发现**:
- 双层确认系统：服务层(PlanItemStreamParser) + UI层(RunCommandCard) 完全独立
- `ew.confirm()` 只是日志打点，真正执行是 `eE(Confirmed)`
- ALWAYS_RUN + RedList 仍然弹窗（未覆盖）
- 17个 Alert 渲染点中只有1个被补丁覆盖（可继续错误）
- source-architecture.md 关键位置索引从7个扩展到38个

