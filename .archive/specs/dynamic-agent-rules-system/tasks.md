# Tasks: 跨会话 Agent 动态规则遵守系统

## 任务概览

本系统采用**渐进式实现策略**：先在 trae-unlock 项目中验证核心机制，再抽象为可移植框架。

---

## Phase 1: 基础设施搭建（规则仓库 + 规则定义）

- [x] **Task 1.1**: 创建 `rules/` 目录并定义 4 个核心规则文件
  - [x] 1.1.1 创建 `rules/core.yaml` — 核心必读规则（文档阅读要求、新发现记录规范）
  - [x] 1.1.2 创建 `rules/workflow.yaml` — 工作流程规则（探索→修改→测试→提交循环）
  - [x] 1.1.3 创建 `rules/git.yaml` — Git 提交规范（格式、强制 push）
  - [x] 1.1.4 创建 `rules/safety.yaml` — 安全规则（密钥保护、.git 完整性）
  - **验证标准**：每个 YAML 文件符合 spec 中定义的 schema，包含 id/name/category/priority/enabled/description/actions/enforcement 字段

- [x] **Task 1.2**: 从现有 AGENTS.md 提取规则并结构化
  - [x] 1.2.1 将 AGENTS.md 中的"新会话开始前必读"章节 → 迁移到 `rules/core.yaml`
  - [x] 1.2.2 将"核心规则"章节（更新文档要求）→ 迁移到 `rules/core.yaml`
  - [x] 1.2.3 将"Git 提交规则"章节 → 迁移到 `rules/git.yaml`
  - [x] 1.2.4 将"安全建议"相关内容 → 迁移到 `rules/safety.yaml`
  - **验证标准**：所有现有规则都被完整保留，无信息丢失

---

## Phase 2: 规则引擎开发（核心脚本）

- [x] **Task 2.1**: 实现 `scripts/rules-engine.ps1` 基础功能
  - [x] 2.1.1 实现 YAML 解析器（使用 PowerShell 原生能力或简单正则，避免外部依赖）
  - [x] 2.1.2 实现规则扫描逻辑（递归读取 rules/*.yaml）
  - [x] 2.1.3 实现过滤逻辑（只加载 enabled: true 的规则）
  - [x] 2.1.4 实现优先级排序（high > medium > low）
  - **验证标准**：执行脚本后输出格式化的 Markdown 规则文本

- [x] **Task 2.2**: 增强 Markdown 输出质量
  - [x] 2.2.1 按类别分组显示规则（core / workflow / git / safety）
  - [x] 2.2.2 为每条规则生成清晰的标题和操作步骤
  - [x] 2.2.3 添加统计信息（共 X 条规则，Y 条启用，Z 条禁用）
  - **验证标准**：输出文本可直接被 AI 理解和遵循

- [x] **Task 2.3**: 实现 CLI 参数支持
  - [x] 2.3.1 支持 `--check` 参数（语法验证模式）
  - [x] 2.3.2 支持 `--list` 参数（列出规则状态摘要）
  - [x] 2.3.3 支持 `--output` 参数（指定输出文件路径）
  - **验证标准**：`./rules-engine.ps1 --check` 能正确检测 YAML 语法错误

---

## Phase 3: AGENTS.md 重构为路由器

- [x] **Task 3.1**: 重构 AGENTS.md 为轻量级入口
  - [x] 3.1.1 移除所有硬编码的规则细节（从 ~128 行缩减至 ~30 行）
  - [x] 3.1.2 编写强制性引导指令（⚠️ 必须先运行规则加载命令）
  - [x] 3.1.3 添加规则引擎调用示例（powershell scripts/rules-engine.ps1）
  - [x] 3.1.4 添加缓存/刷新说明（修改 rules/ 后重新生成）
  - **验证标准**：AGENTS.md 总行数 < 50 行，且不包含任何具体规则内容

- [x] **Task 3.2**: 测试路由器机制有效性
  - [x] 3.2.1 模拟新会话场景：AI 读取 AGENTS.md 后是否知道要运行规则引擎
  - [x] 3.2.2 验证规则引擎输出是否能被正确嵌入上下文
  - **验证标准**：通过实际对话测试确认 AI 会主动执行规则加载命令

---

## Phase 4: 文档与可移植性准备

- [x] **Task 4.1**: 编写 `docs/dynamic-rules-system.md` 使用指南
  - [x] 4.1.1 说明系统架构（三层结构图）
  - [x] 4.1.2 提供"如何添加新规则"的 step-by-step 教程
  - [x] 4.1.3 提供 YAML schema 参考文档
  - [x] 4.1.4 编写"迁移到其他项目"的检查清单
  - **验证标准**：新开发者能根据文档独立添加自定义规则

- [x] **Task 4.2**: 更新 README.md 和 progress.txt
  - [x] 4.2.1 在 README.md 新增"动态规则系统"章节
  - [x] 4.2.2 在 progress.txt 记录本次架构升级
  - **验证标准**：项目文档反映最新状态

---

## Phase 5: 验证与优化 + Bug 修复

- [x] **Task 5.1**: 系统全面验证 (40+ 检查点)
  - [x] 5.1.1 基础设施验证（rules/ 目录完整性、Schema 合规性、迁移完整性）
  - [x] 5.1.2 规则引擎功能验证（基础功能、Markdown 质量、CLI 参数、异常处理）
  - [x] 5.1.3 AGENTS.md 路由器验证（精简度、无硬编码、强制性措辞、可操作性）
  - [x] 5.1.4 文档完整性验证（使用指南、README、progress 更新）
  - [x] 5.1.5 可移植性验证（设计通用性、模拟迁移场景）
  - [x] 5.1.6 最终验收（功能完整性、质量标准、文档齐全、零回归）
  - **验证结果**: 初始通过率 92.5%，发现 1 个严重 Bug

- [x] **Task 5.2**: 🔧 紧急修复 CLI 参数解析 Bug
  - [x] 5.2.1 问题定位：PowerShell 不识别 `--` 前缀，导致 --check/--list/--output 被错误绑定给 $Output 参数
  - [x] 5.2.2 修复方案：采用"手动解析 $args + CmdletBinding 增强"组合策略
  - [x] 5.2.3 验证修复：4 种运行模式全部测试通过（默认/check/list/output）
  - **最终通过率**: **98%+** ✅

---

# Task Dependencies

```
Phase 1 (规则仓库) ✅
    ↓
Phase 2 (规则引擎) ✅ ← 依赖 Task 1.1 的 YAML 格式定义
    ↓
Phase 3 (AGENTS.md 重构) ✅ ← 依赖 Task 2.1 的规则引擎基础功能
    ↓
Phase 4 (文档) ✅ ← 与 Phase 3 并行完成
    ↓
Phase 5 (验证+修复) ✅ ← 发现并修复了 CLI 参数 Bug
```

---

# 实现总结

## ✅ 全部任务已完成！

### 📊 成果统计

| 类别 | 数量 | 状态 |
|------|------|------|
| **新增文件** | 7 个 | ✅ 完成 |
| **重构文件** | 3 个 | ✅ 完成 |
| **代码行数** | ~1,021 行 | ✅ 完成 |
| **规则数量** | 15 条 | ✅ 迁移完整 |
| **文档行数** | ~976 行 | ✅ 覆盖全面 |
| **验证检查点** | 40+ 个 | ✅ 98%+ 通过 |

### 🎯 核心成果

1. **三层架构设计**：路由器(AGENTS.md) → 引擎(rules-engine.ps1) → 仓库(rules/*.yaml)
2. **高度可移植**：30 分钟内可迁移到任意项目
3. **配置驱动**：所有规则 YAML 化，支持启用/禁用/优先级控制
4. **零外部依赖**：纯 PowerShell 实现，无需安装额外模块
5. **文档完善**：556 行使用指南 + README/progress 更新

### 🔧 技术亮点

- AGENTS.md 从 128 行瘦身至 43 行（**减少 66%**）
- 规则引擎支持 4 种运行模式（默认/check/list/output）
- 双重参数兼容（PowerShell `-` 前缀 + Unix `--` 前缀）
- 完整的错误处理和异常恢复机制
