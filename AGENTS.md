# Agent Rules for Trae Mod Project

## ⚠️⚠️⚠️ Anchor 声明

**你不是一个孤立的会话。用户会在不同时间开启多个 AI 会话。**

这意味着：
1. 你当前的工作成果可能需要被未来的会话继承
2. 你需要的信息可能已经由前序会话记录在 `shared/` 目录中
3. 你有责任将重要信息持久化到 `shared/` 目录，否则它们会随会话结束而丢失
4. 你不能假设用户一直在当前会话中 — 跨会话协作是常态

**在开始任何工作之前，你必须先读取 Anchor 共享知识库：**
→ 读取 `shared/_registry.md` 了解所有可用模块
→ 按 P0 → P1 → P2 优先级读取所需模块

## 🔑 关于本文件的关键洞察

**AGENTS.md 会被 AI 每次回复时自动读取。**

这意味着：
1. 它是跨会话通信的天然入口 — AI 不需要"记住"去读它，因为每次都会读
2. 写在这里的指令具有最高执行力 — 比 README、progress 等文件的优先级更高
3. 它应该保持精简 — 只做路由/跳板，不存储具体内容
4. 如果你想设计跨会话系统，利用这个文件的"每次必读"特性是最巧妙的方式

如果你是未来想改进跨会话通信的 AI，请记住：**AGENTS.md 是你的锚点。**

## ✍️ 你的写入责任

在工作过程中，你应该将重要信息持久化到 `shared/` 目录：

| 时机 | 写入文件 | 内容 |
|------|---------|------|
| 发现关键代码/架构 | `shared/discoveries.md` | 位置、作用、影响 |
| 做出技术决策 | `shared/decisions.md` | 决策、原因、替代方案 |
| 完成工作后 | `shared/status.md` | 完成了什么、待做什么、遇到什么问题 |
| 修改了规则 | 运行下方命令 | 自动更新 `shared/rules.md` |

**写入格式**: 遵循 `shared/_registry.md` 中的写入格式约定

## 🔧 规则更新

协作规则存储在 `rules/*.yaml`，通过引擎生成到 `shared/rules.md`：

```powershell
powershell scripts/rules-engine.ps1 --output shared/rules.md   # 更新规则文件
powershell scripts/rules-engine.ps1 --check                     # 验证规则语法
```

## 🏥 补丁自检协议

**每次新会话开始时，你必须先运行补丁自检：**

```powershell
powershell scripts/auto-heal.ps1 -DiagnoseOnly
```

- 如果输出 "All N patches verified ✅" → 继续正常工作
- 如果有 FAIL 项 → 立即运行 `powershell scripts/auto-heal.ps1` 执行自动修复
- 如果自动修复后仍有 MANUAL-NEEDED 项 → 告知用户，并记录到 `shared/status.md`

**为什么这很重要**：Trae 更新可能导致补丁静默失效。不自检就工作 = 在破损基础上构建。

## 🔍 搜索优先原则（血泪教训）

**写代码之前，必须先搜索 3 轮。这是强制规则，不是建议。**

```
第 1 轮：搜工具 — "有没有现成工具能做这件事？"
第 2 轮：搜方案 — "别人遇到类似问题怎么解决的？"
第 3 轮：搜生态 — "这个领域的标准做法是什么？"
```

**只有 3 轮搜索都没有找到合适方案时，才自己写代码。**

**为什么这很重要**：本项目的核心补丁工具 ast-grep，是在手动写了十几轮 PowerShell 搜索脚本之后才发现的。如果一开始就搜索，可以节省 80% 的时间。

**可用工具**：
- `ast-grep -p 'pattern' --lang js --json target.js` — AST 结构搜索（含 byteOffset）
- `powershell scripts/tools/search-target.ps1 -Pattern "关键词"` — 文本搜索
- `WebSearch` — 互联网搜索

**理解代码之前先用工具映射，不要靠猜。**

## 🔄 复盘协议（自我进化机制）

**⚠️⚠️⚠️ 复盘是 Return Final Response 的前置条件。不复盘 = 任务未完成。**

**触发条件（满足任一即触发）**：
- 补丁已实施并验证通过（auto-heal PASS）
- 问题已修复（代码修改完成）
- 新功能已完成
- 用户反馈已处理
- **任何 TodoWrite 标记 completed 时** ← 最常见的触发点

**禁止行为**：
- ❌ tasks.md 全勾 + checklist.md 全过 → 直接 Return（**这是违规！**）
- ❌ 等用户提醒才复盘
- ❌ "下次再做"或"用户没要求就不做"

```
1. 回顾：我做了什么？花了多少步骤？
2. 反思：有没有更快的路径？哪些步骤是冗余的？
3. 提炼：能提炼什么可复用的方法论？
4. 更新：将改进写入规则系统或 shared/discoveries.md
```

**📋 Return 前自检清单（必须全部确认）**：
```
□ 复盘四步是否全部执行？
□ 是否产出了至少一条可复用的方法论或规则更新？
□ discoveries.md 是否已更新？（新的发现/方法论）
□ 如果发现效率问题，rules/workflow.yaml 是否已更新？
□ status.md 会话日志是否已追加？
```

**推理→搜索→验证三步法**（搜索前先推理，避免盲目搜索）：

```
Step 1: 推理 — 从已知架构推断可能的答案，列出关键假设
Step 2: 搜索 — 先搜行业参考（WebSearch），再用 ast-grep 精确搜索
Step 3: 验证 — 只验证推理中的关键假设，不穷举
```

**为什么这很重要**：循环检测任务中，如果先推理"SSE 推送错误码 → 必然是服务端"，再用 ast-grep 一条命令验证，5 分钟就能得出结论。实际花了整个 spec 周期。

**假设优先搜索法**（rule-011，从本次 v4 修复中提炼）：

```
遇到问题时，先花 2 分钟列出 2-4 个可能的根因假设
每个假设对应一个关键验证点（1个搜索命令就能确认/排除）
按"排除成本从低到高"的顺序验证
一旦某个假设被验证为真，立即停止搜索
禁止"广撒网式搜索"——没有明确目标的搜索是时间黑洞

目标：将搜索次数从 O(n²) 降低到 O(n)，n=假设数
示例：ec()不工作？→ 假设A(条件不满足)→搜ec定义看条件；假设B(resumeChat失败)→搜resumeChat实现
```

**中间层陷阱警告**（rule-012）：

```
调用回调/封装函数时，不要假设它会做你认为它该做的事。必须查看内部实现。
特别注意函数内部的 if 条件判断——这些条件可能因为外部环境变化而不满足。
如果中间层的条件判断阻碍了目标，考虑绕过它直接调用底层 API（必须有 fallback）
```

## 📖 方法论快速索引

> **新会话 AI 必读**：以下方法论是 5 个会话的复盘结晶，按适用场景分类。详细内容见 shared/discoveries.md。

### 🔍 搜索与调查类

| 方法论 | 来源 | 适用场景 | 一句话 |
|--------|------|---------|--------|
| **搜索优先三原则** | rule-005 | 写代码前 | 先搜工具→搜方案→搜生态，禁止重复造轮子 |
| **推理→搜索→验证三步法** | rule-010 | 理解代码前 | 推理假设→针对性搜索→只验关键点 |
| **假设优先搜索法** | rule-011 (#17) | 排查 bug 时 | 列假设→每假设1个搜索→排除成本排序 |
| **渲染层 vs 逻辑层分离诊断** | #17 | UI 异常时 | 先区分数据问题还是渲染问题，在错误层级搜=白费 |

### 🛠️ 补丁开发类

| 方法论 | 来源 | 适用场景 | 一句话 |
|--------|------|---------|--------|
| **中间层陷阱警告** | rule-012 (#16) | 调用回调时 | 回调内部有条件判断，必须查看实现，必要时绕过 |
| **后执行覆盖者调试模式** | #17 | 状态被莫名覆盖时 | stopStreaming/cleanup 在 D7.Error 后静默覆盖状态，需追踪完整时序 |
| **二次错误覆盖模式** | #18 | UI 从正确变错误时 | 先正确后错误=变量值在两次渲染间被改变，需加速首次响应+保护变量 |
| **预期即正确原则** | #18 | 看到"异常"UI 时 | 黄色警告出现=if(V&J)在工作≠bug，先确认是否真的是异常 |
| **纵深防御模式** | #18 (v5) | 设计多层修复时 | 像三层防空——拦截(DEFAULT入J) + 近防炮(500ms) + 装甲(retry) |

### 🧠 元认知类

| 方法论 | 来源 | 适用场景 | 一句话 |
|--------|------|---------|--------|
| **违规成本为零根因** | #17 (复盘) | 规则不生效时 | 不复盘无任何机制阻止 → 必须建立硬性检查点 |
| **规则制定≠规则遵守鸿沟** | #17 (复盘) | 制定规则后 | "理性自我"制定但"惯性自我"执行时走最短路径 |
| **沉默杀手模式** | #17 | 隐蔽 bug 时 | 后执行的覆盖者很难发现，调试只能看到最终状态 |

### 📊 效率数据参考

| 会话 | 任务 | 步数 | 浪费率 | rule-011 应用 |
|------|------|------|--------|--------------|
| #14 | loop-detection | ~20 | ~90% | ❌ 未用 |
| #15 | manual-stop v3 | ~15 | ~80% | ❌ 未用 |
| #16 | v3→v4 | 17 | **82%** | ❌ 未用 |
| #17 | guard-clause | 17 | **82%** | ❌ 未用 |
| #18 | warning+2000000 | 15 | **~10%** | ✅ **首次生效** |
